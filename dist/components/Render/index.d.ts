declare type RenderProps = {
    children: any;
    if: any;
};
/***
 * Conditionally renders children if `condition` is truthy.
 * @param props
 */
export declare const Render: (props: RenderProps) => any;
export {};
